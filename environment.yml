name: deeplsh
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip=23.3.1
  - numpy=1.19.5
  - pandas=1.2.4
  - scikit-learn=0.24.2
  - scipy=1.7.0
  - h5py=3.1.0
  - grpcio=1.34.1
  - matplotlib=3.4.2
  - seaborn=0.11.1
  - requests
  - chardet
  - pip:
      # Apple Silicon native TensorFlow.
      # Note: Using upstream `tensorflow==2.5.0` on x86_64 requires AVX and will not run under Rosetta.
      - tensorflow-macos==2.5.0
      # Required by code/python-packages
      - pylev==1.4.0
      - fnvhash==0.2.1
