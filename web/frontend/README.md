# Web Frontend

Vue 3 + Vite + Element Plus frontend for the network intrusion detection log deduplication prototype.

## Run

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8080` and proxies `/api` during local development.
